# Set the seed for reproducibility
set.seed(123)

# Create a vector of 10 random integers between -50 and +50
rand_ints <- sample(-50:50, 10, replace = TRUE)

# Print the vector
print(rand_ints)
